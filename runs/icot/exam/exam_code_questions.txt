Code Questions Index for ICoT Multiplication Exam
===================================================

[code_id]: CQ1
Title: Fourier Basis R² Verification
Prompt: Verify the claim about Fourier basis R² fits. Implement a function that: (1) generates 10 random digit embedding vectors (simulating embeddings for digits 0-9 with dimension d=768), (2) constructs the Fourier basis matrix with frequencies k ∈ {0,1,2,5} plus parity, (3) computes the R² fit, and (4) demonstrates that structured embeddings (using the Fourier basis) achieve near-perfect R² while random embeddings achieve much lower R². The Fourier basis for digit n is: Φ(n) = [1, cos(2πn/10), sin(2πn/10), cos(2πn/5), sin(2πn/5), (-1)^n].
Expected_Outcome: The solution should print two R² values: (1) R² for structured embeddings (built from Fourier basis) ≈ 1.0 (very close to perfect fit, typically > 0.99), and (2) R² for random embeddings < 0.5 (poor fit). The difference demonstrates that embeddings with Fourier structure are perfectly explained by the Fourier basis, while random embeddings are not. Typical output: "Structured embeddings R²: 1.0000" and "Random embeddings R²: 0.23" (tolerance: structured > 0.98, random < 0.6).
Reference: Section 4: Fourier Basis Embeddings and Fourier Fit Quality (median R² values for embeddings, MLP weights, and hidden states)
---

[code_id]: CQ2
Title: Attention Tree Caching/Retrieval Simulation
Prompt: Simulate and verify the attention tree caching/retrieval mechanism. Implement a simplified version where: (1) Layer 1 'caches' pairwise products aᵢ * bⱼ in a dictionary keyed by timestep, (2) Layer 2 'retrieves' these values using attention-like indexing to compute a specific output digit cₖ, and (3) demonstrate that this tree structure correctly computes c₃ for the example 8331 × 5015 (input format: 1338 * 5105), showing which cached products are retrieved. Mathematical background: c₃ requires partial products a₃b₀, a₂b₁, a₁b₂, a₀b₃, plus carry from c₂. Each digit cₖ = (Σ aᵢbⱼ for i+j=k, plus carry from k-1) mod 10.
Expected_Outcome: The solution should: (1) print the cached products at each timestep showing Layer 1's caching mechanism, (2) print which specific products are retrieved for computing c₃, (3) compute and print the correct value c₃ = 7. This verifies the attention tree structure by showing how intermediate partial products are cached and later retrieved. The actual product 8331 × 5015 = 41797665, so c₃ (4th digit from right in reversed format) = 7. Output should explicitly show retrieved products: a₃b₀=40, a₂b₁=3, a₁b₂=0, a₀b₃=5, and their sum with carry yielding c₃=7 (tolerance: exact match required, c₃ must equal 7).
Reference: Section 4: Discovered Mechanisms - Attention Tree Structure (Layer 1 caching, Layer 2 retrieval, example for c₂ computation)
---

[code_id]: CQ3
Title: Logit Attribution Dependency Pattern Verification
Prompt: Verify the logit attribution dependency pattern. For a sample 4×4 multiplication, implement a function that: (1) computes which input digit positions (aᵢ, bⱼ) should affect which output digit cₖ according to the mathematical structure of multiplication, (2) creates a dependency matrix showing this structure, (3) validates that cₖ depends on partial products where i+j ≤ k, and (4) shows that the strongest dependencies occur when i+j = k (direct contribution without carry).
Expected_Outcome: The solution should produce an 8×8 dependency matrix (8 input positions [a₀,a₁,a₂,a₃,b₀,b₁,b₂,b₃] × 8 output positions [c₀,c₁,c₂,c₃,c₄,c₅,c₆,c₇]) with values: 2 for strong dependency (i+j=k), 1 for weak dependency (i+j<k via carry), 0 for no dependency (i+j>k). The matrix should show: (1) c₀ depends strongly (value=2) only on a₀ and b₀, (2) c₇ has at least weak dependencies on all inputs, (3) middle digits have multiple strong dependencies, (4) the pattern matches the mathematical structure of multiplication. A heatmap visualization should clearly show the diagonal pattern of strong dependencies. Validation should confirm these properties and print "All dependency validations passed" (tolerance: exact integer values 0/1/2 in matrix, validation checks must all pass).
Reference: Section 4: Logit Attribution Results - ICoT shows correct dependencies where aᵢ, bⱼ affect cₖ when k ≥ i+j, strongest when i+j=k
---
